import 'package:flutter/material.dart';

class FullTicket extends StatelessWidget {
  const FullTicket({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
